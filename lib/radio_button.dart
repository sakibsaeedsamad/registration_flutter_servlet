import 'package:flutter/material.dart';

import 'gender_model.dart';
import 'main.dart';

class RadioButton extends StatelessWidget {

  late List<Gender> genderList;
  RadioButton(this.genderList);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
